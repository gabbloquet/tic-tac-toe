import {calculateNextValue, calculateStatus, calculateWinner} from './Game.service'

describe('Game tests', () => {
  describe('Calcultate next player', () => {
    it('should return next player when TODO', () => {

    });
  });

  describe('Calcultate Status', () => {
    it('should return status when TODO', () => {

    });
  });

  describe('Calcultate Winner', () => {
    it('should return winner when TODO', () => {

    });
  });
});
